/**
 * AdminDashboard — analytics and oversight.
 *
 * TODO:
 *   - Fetch stats via getDashboardStats()
 *   - Fetch department stats via getDepartmentStats()
 *   - Fetch category distribution via getCategoryDistribution()
 *   - KPI cards: total, resolved, pending, avg resolution time
 *   - Bar chart: department breakdown (recharts)
 *   - Pie chart: category distribution (recharts)
 *   - HotspotMap component showing complaint density
 *   - Paginated complaint table
 */

export default function AdminDashboard() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">Admin Dashboard</h1>
      <p className="text-gray-500">TODO: AdminDashboard — KPI cards, charts, map, and complaint table</p>
    </div>
  );
}
