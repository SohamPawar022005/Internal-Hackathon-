/**
 * OfficerDashboard — manage assigned complaints.
 *
 * TODO:
 *   - Fetch officer's complaints via getOfficerComplaints()
 *   - Show table/list with PriorityBadge, SLABadge per row
 *   - Click to expand → update status, add comment
 *   - Filter by status, sort by priority
 */

export default function OfficerDashboard() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">Officer Dashboard</h1>
      <p className="text-gray-500">TODO: OfficerDashboard — complaint list with status update controls</p>
    </div>
  );
}
