/**
 * HotspotMap — Leaflet map with heatmap overlay showing complaint density.
 *
 * TODO:
 *   - Fetch geo data via getGeoComplaints() and getHeatmapData()
 *   - Render Leaflet map centered on Delhi (28.6139, 77.2090)
 *   - Add marker layer for individual complaints
 *   - Add heatmap layer using leaflet.heat
 *   - Color-code markers by priority level
 */

export default function HotspotMap() {
  return (
    <div className="bg-gray-100 rounded-lg p-4 h-96 flex items-center justify-center">
      <p className="text-gray-500">TODO: HotspotMap — Leaflet + leaflet.heat complaint density map</p>
    </div>
  );
}
